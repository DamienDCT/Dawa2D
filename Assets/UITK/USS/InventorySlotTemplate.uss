:root {
}

.slot-image {
    position: absolute;
    width: 50%;
    height: 50%;
    justify-content: flex-start;
    align-self: center;
    align-content: flex-start;
    align-items: center;
    background-color: rgba(246, 255, 0, 0);
}
